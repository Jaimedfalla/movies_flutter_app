import 'package:flutter/material.dart';

class MoviesTheme{
    static final ThemeData moviesTheme = ThemeData.light().copyWith(
      appBarTheme: const AppBarTheme(
        color: Colors.indigo,
        elevation: 0
      )
    );
}